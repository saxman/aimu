"""Google Gemini clients: text (via the OpenAI-compat endpoint) and image (google-genai).

Text subclasses :class:`aimu.models.providers.openai_compat.OpenAICompatClient`; image
uses the native ``google-genai`` SDK. Mirrors ``aimu.aio.providers.gemini``.
"""
