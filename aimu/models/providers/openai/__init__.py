"""OpenAI cloud clients: text (GPT / o-series) and speech (TTS).

The text client subclasses :class:`aimu.models.providers.openai_compat.OpenAICompatClient`
(OpenAI is the canonical OpenAI-compatible endpoint); the speech client is its own
:class:`aimu.models.base.BaseSpeechClient`. Mirrors ``aimu.aio.providers.openai``.
"""
